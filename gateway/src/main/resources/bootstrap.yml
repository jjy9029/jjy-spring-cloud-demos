spring:
  application:
    name: gateway
  profiles:
    active:
      dev
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        username: nacos
        password: nacos
        group: DEFAULT_GROUP
      config:
        server-addr: http://localhost:8848
        file-extension: yaml
        shared-configs:
